<h1>Items</h1>
<% @items.each do |item| %>
   <div class="media">
     <div class="media-body">
       <h4 class="media-heading">
         <%= link_to item.title, item %>
       </h4>
     </div>
   </div>
<% end %>